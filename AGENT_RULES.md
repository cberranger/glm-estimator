# Agent Rules — glm-estimator

## Project Overview
Next.js application for estimating/construction workflows.

## Build Commands
- `npm run dev` — Start development server
- `npm run build` — Production build

## Notes
- Uses Next.js 15.x (webpack, not Turbopack due to Windows junction issues)
- Connected to Universal Error Shipping via logs/error.log
